/// The project's version. `scripts/build-app.sh` reads this string to stamp the app bundle,
/// so this declaration is the single source of truth.
public enum AnchorVersion {
    public static let marketing = "1.0"
}
